{
  "name": "bp-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Blueprint Map — Cloudflare Worker proxy for Google Sheets reads.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0",
    "wrangler": "^4"
  },
  "dependencies": {
    "jose": "^5.9.0"
  }
}
